import { LessonHighlightDirective } from './lesson-highlight.directive';

describe('LessonHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new LessonHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
